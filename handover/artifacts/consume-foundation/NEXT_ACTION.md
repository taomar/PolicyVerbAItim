# Next action

The exact T1 patch is now attributed and narrowly approved.

1. Confirm the earlier Rule/backend and Payload prerequisites are independently cleared.
2. Apply `03-consume-request-mode-foundation.patch` to clean HEAD `c0972c8bb7ba811903e981d3806afe31cc1f514d`.
3. Apply `04-consume-native-parser-renderer.patch` immediately after it.
4. Require the hashes, path sets, and validation results recorded in `PATH_HUNK_MANIFEST.json` and `SEQUENCE_VALIDATION.json`.

Do not stage the four mixed files whole. The approval covers only the exact ordinal-3 patch bytes and does not approve any other edit from the archived authoring session.
